#include <stdio.h>

int main()
{
    int sum = 0;
    int n = 10;
    
    for (int i = 1 ; i <= n ; ++i)
    {
        sum += i;
    }
    // printf("Sum of numbers from 1 to %d is %d", n, sum);
    return 0;
}
